#include <iostream>
#include <map>
#include <string>

using namespace std;

int main(){
  map<string> byr;
  string info[1000];

  for(int i=0; i<1000; i++){
    getline(cin, info[i]);
  }

  for(int j=0; j<1000; j++){
    if()
  }
}
