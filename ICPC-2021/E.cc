#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const double PI = acos(-1.0);
#define INF 1000000000

#define all(X) begin(X), end(X)


int main() {
  int n;
  cin >> n;
  vector<int> p(n);
  for(int i=0; i<n;i++) {
    cin >> p[i];
  }
  sort(all(p));
  for(int i=0; i<n/2; i++) {
    
  }


  //cout:
  cout << " ";
  //cin:
  cin >> "";
  //debug cerr

  //round up
  (A+(B-1)) /B

  return 0;
}
