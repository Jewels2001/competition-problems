#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const double PI = acos(-1.0);
#define INF 1000000000


int main() {
    int m, a,b,c;
    cin >> m >> a >> b >> c;
    int test = m;
    test = ((a+ b) + (a+c) + (b+c));

    cout << (test >=)
    if(test >= 0) {
    
    }
  //cout:
  cout << " ";
  //cin:
  cin >> "";
  //debug cerr

  //round up
  (A+(B-1)) /B

  return 0;
}