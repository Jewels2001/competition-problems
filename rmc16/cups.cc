#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const double PI = acos(-1.0);
#define INF 1000000000


int main() {
  int n;
  cin >> n;
  vector<string> a;
  cin.ignore(100, '\n');
  for(int i=0;i<n;i++) {
    string temp;
    getline(cin, temp);
    istringstream ss(temp);
    while(ss >> temp) {
      if(isdigit(temp[0])) {

      }
    }

  }
  //cout:
  cout << " ";
  //cin:
  cin >> "";

  //round up
  (A+(B-1)) /B

  return 0;
}
