#include <bits/stdc++.h>
using namespace std;

int main() {
  int cases;
  cin >> cases;
  for(int i=0; i<cases;i++) {
    int n;
    cin >> n;
    bool odd;
    int total = 0;
    priority_queue<int> pq1;
    priority_queue<int> pq2;
    for(int j=0; j<n;j++){
      int a;
      cin >> a;
      pq1.push(a);
      
      odd
    }

    sort()

    if(n%2){
      current +=
    }
    current += /2;
    //cout:
    cout << " ";
    //cin:
    cin >> "";

    //round up
    (A+(B-1)) /B

  }
  return 0;
}
