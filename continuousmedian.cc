#include <bits/stdc++.h>
using namespace std;

int main() {
  int cases;
  cin >> cases;
  for(int i=0; i<cases;i++) {
    int n;
    cin >> n;
    int current = 0;
    int total = 0;
    int array[n];
    for(int j=0; j<n;j++){
      int a;
      if(array[j] <)
      cin >> array[j];

    }

    sort()

    if(n%2){
      current +=
    }
    current += /2;
    //cout:
    cout << " ";
    //cin:
    cin >> "";

    //round up
    (A+(B-1)) /B

  }
  return 0;
}
