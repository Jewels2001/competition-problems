//with cases
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const double PI = acos(-1.0);
#define INF 1000000000

int main() {
  int n, l, p;
  cin >> n >> l >> p;
  double ans = 0.00;
  for(int i=0; i<n; i++) {

    for(int j=0; j<l;j++) {
      ans += 0.5*l;
    }
  }

    cout << " ";


    //round up
    (A+(B-1)) /B


  return 0;
}
