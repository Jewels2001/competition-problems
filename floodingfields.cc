#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const double PI = acos(-1.0);
#define INF 1000000000


// graph will flow through time,
// nodes will be connected to where it flows next

// capacity of flow is 1 on every node


f() {
  
}


int main() {
  int n, k, h;
  cin >> n >> k >> h;
  vector<vector<int>> Graph(n, vector<int>());

  for(int i=0; i<n;i++) {

  }
  //cout:
  cout << " ";
  //cin:
  cin >> "";
  //debug cerr

  //round up
  (A+(B-1)) /B

  return 0;
}
