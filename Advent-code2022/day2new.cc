#include <bits/stdc++.h>
using namespace std;

enum Letters { A, B, C, X, Y, Z };

// try one enum for each set of letters?

int main() {
    Letters a, b;
    int total = 0;
    while(cin >> a >> b) {
        
    }
}