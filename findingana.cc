#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const double PI = acos(-1.0);
#define INF 1000000000
#define all(X) begin(X), end(X)

int main() {
  string a;
  cin >> a;
  string ans = "";
  if(find(all(a), 'a') != std::string::npos) {

  }
  //cout:
  cout << " ";
  //cin:
  cin >> "";
  //debug cerr

  //round up
  (A+(B-1)) /B

  return 0;
}
