#include <bits/stdc++.h>
using namespace std;

int size = 24;

int main() {
  stack<char> syntax;

  //input
  string chunk;
  while(cin >> chunk) {
    stringstream ss(chunk);
    char temp;
    while(ss >> temp) {
      
    }
    if()
  }
  return 0;
}
