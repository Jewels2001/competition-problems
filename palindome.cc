
#include iostream;
#include cmath;
using namespace std;

int main(){
  int n;
  int p;
  int k; //non negative less than p
  int x;  // iff x = k % p (remainder x/p=k)

  int answer;
  cin >> n >> p >> k;
  x = k%p;
  int good[]
  assert(x/p = k);



  answer = x % (10^9 + 7);

  cout << answer;

  return 0;
}
