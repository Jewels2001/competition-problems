#include <bits/stdc++.h>
using namespace std;

//long long
typedef long long ll;
const double PI = acos(-1.0);

int main() {
  int a, b, t, ta, tb, c;
  cin >> a >> b >> t >> ta >> tb;
  int ans = 0;
  cin >> c;

  priority_queue<pair<int, int>> p;
  //tech, busniess

  for(int i=1; i<=c; i++) {
    pair<int,int> t;
    cin >> t.first >> t.second;
    p.push(t);
  }
  //cout:
  cout << " ";
  //cin:
  cin >> "";

  //round up
  (A+(B-1)) /B

  return 0;
}
