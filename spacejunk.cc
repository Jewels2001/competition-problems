#include <bits/stdc++.h>
using namespace std;

int main() {
  int cases;
  cin >> cases;
  for(int i=0; i<cases;i++) {
    double sx,sy,sz,sjx,sjy,sjz;
    double  svx, svy, svz, sjvx, sjvy, sjvz;
    cin >> sx >> sy >> sz >> svx >> svy >> svz;
    cin >> sjx >> sjy >> sjz >> sjvx >> sjvy >> sjvz;

    //x^2 + y^2 < c^2
    double a = (sx - sjx)
    //cout:
    cout << " ";
    //cin:
    cin >> "";


  }
  return 0;
}
